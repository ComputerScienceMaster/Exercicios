/*

5)  Implemente uma função recursiva para contar o número de ocorrências de um determinado caractere
em uma cadeias de caracteres. A função deve obedecer ao seguinte protótipo:

int conta_r(char *str1, char ch);

Escreva também função recursiva para comparar duas cadeias de caracteres. A função deve retorna 1 se
as duas cadeias forem iguais ou 0 se elas forem diferentes. A função deve obedecer ao seguinte protótipo:

int compara_r(char *str1, char *str2);

Escreva também uma função recursiva para procurar um caractere em uma cadeia de caracteres. Caso o caractere
seja encontrado, a função deve retornar 1, caso contrário, deve retornar 0. A função deve obedecer ao seguinte protótipo:

int busca_r(char *str, char c);

Implemente também uma função recursiva para substitui todas as ocorrências de letras minúsculas na cadeia pela equivalente
em maiúsculo, modificando a palavra original. A função deve obedecer ao seguinte protótipo:

void maiusculo_r(char *str, char c);

IMPORTANTE: Todas as funções anteriores devem ser implementadas de forma recursiva. Em seguida, escreva a função
principal do programa para ler uma cadeia de caracteres e realizar todas as operações recursivas implementadas,
mostrando o resultado produzido pelas funções.

*/




main(){

}
